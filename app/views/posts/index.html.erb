<p id="notice"><%= notice %></p>

<h2>Listing Posts</h2>


<% @posts.each do |post| %>
<!---      <tr>
        <td><%= post.user.email %></td>
        <td><%= post.title %></td>
        <td><%= truncate(strip_tags(post.body), length: 80) %></td>
        <td><%= link_to 'Show', post %></td>
        <%if post.user.email==current_user.email or current_user.id==1%>
          <td><%= link_to 'Edit', edit_post_path(post) %></td>
          <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <%end%>
      </tr> -->
  <div class = "one">
    <strong><%= link_to "#{post.title}", post %></strong><br>
    <small><%= post.user.email %>, <%= post.created_at %></small><br>    
    <%= truncate(strip_tags(post.body), length: 80) %>
  </div>
  <br>
<% end %>
